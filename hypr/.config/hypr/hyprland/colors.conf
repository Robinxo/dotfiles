general {
    col.active_border = rgba(899294AA)
    col.inactive_border = rgba(3f484aAA)
}

misc {
    background_color = rgba(0e1415FF)
}

plugin {
    hyprbars {
        # Honestly idk if it works like css, but well, why not
        bar_text_font = Rubik, Geist, AR One Sans, Reddit Sans, Inter, Roboto, Ubuntu, Noto Sans, sans-serif
        bar_height = 30
        bar_padding = 10
        bar_button_padding = 5
        bar_precedence_over_border = true
        bar_part_of_window = true

        bar_color = rgba(0e1415FF)
        col.text = rgba(dee3e5FF)


        # example buttons (R -> L)
        # hyprbars-button = color, size, on-click
        hyprbars-button = rgb(dee3e5), 13, 󰖭, hyprctl dispatch killactive
        hyprbars-button = rgb(dee3e5), 13, 󰖯, hyprctl dispatch fullscreen 1
        hyprbars-button = rgb(dee3e5), 13, 󰖰, hyprctl dispatch movetoworkspacesilent special
    }
}

windowrulev2 = bordercolor rgba(82d3e0AA) rgba(82d3e077),pinned:1
